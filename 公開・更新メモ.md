# びあけん対策ドリル 公開・更新メモ

このアプリを GitHub Pages で公開・更新・iPhone利用するための手順まとめ。

## 基本情報

| 項目 | 内容 |
|------|------|
| 公開URL（本番） | https://marchisio17.github.io/biaken-drill/ |
| GitHubリポジトリ | https://github.com/marchisio17/biaken-drill （public） |
| 公開方式 | GitHub Pages（Deploy from a branch / `main` / `/ (root)`） |
| 公開フォルダ（編集元） | `~/biaken-drill`（＝公開している本体。今後はここを編集する） |
| 開発控え | `ビールプロジェクト/07_勉強アプリ/`（ローカル作業用の控え。公開には未使用） |

> ⚠️ アプリは `~/biaken-drill` と `ビールプロジェクト/07_勉強アプリ/` の**2か所**にある。
> **公開に反映されるのは `~/biaken-drill` 側だけ**。更新は必ず `~/biaken-drill` を編集すること。

---

## アプリを更新して本番に反映する（いちばん使う手順）

1. `~/biaken-drill` の中のファイルを編集（例：`questions.js` に問題を追加）
2. ターミナルで以下を実行：

```bash
cd ~/biaken-drill
git add -A
git commit -m "更新内容をここに書く（例：問題を5問追加）"
git push
```

3. 数十秒〜数分で本番URLに反映される
4. iPhone側は、ホーム画面のアプリを一度閉じて開き直すと最新になる
   （すぐ反映されない時は、Safariで一度URLを開き直す）

### バージョン表示について

- アプリのホーム最下部に `v1.1.0 ・ 全○問` のようにバージョンと収録問題数が出る。
- 更新を出すときは、わかりやすさのため次の2か所の数字を上げておくとよい：
  - `app.js` の `APP_VERSION`（画面に出る表示用バージョン）
  - `service-worker.js` の `CACHE_VERSION`（オフラインキャッシュを確実に更新するための番号）
- 問題数の表示は自動カウントなので、問題を足したら手で直す必要はない。

---

## 問題を追加する（questions.js の書き方）

`~/biaken-drill/questions.js` の配列末尾に追記する。

```js
{
  id: 15,                       // 既存とかぶらない数字
  chapter: 2,                   // テキストの章番号（1〜13）。下の対応表参照
  category: "原料",             // 原料 / 製法 / スタイル / 歴史 / 楽しみ方 / その他
  level: 3,                     // 級の目安（3 / 2 / 1）
  question: "問題文",
  choices: ["選択肢1", "選択肢2", "選択肢3", "選択肢4"],  // 必ず4つ
  answer: 0,                    // 正解の番号。0=1番目, 3=4番目（正解は1番目に書いてOK。表示時に自動でシャッフルされる）
  explanation: "解説"
},
```

章番号（chapter）の対応表：

```
1=ビールとは            2=ビールの原料          3=ビールの製造工程
4=ビールの世界史        5=ビールの日本史        6=日本の酒税法とビール
7=ビール文化と触れ合う場 8=ビール文化を支える団体 9=ビールの消費動向
10=多様なビアスタイル    11=ビールのおいしさ     12=ビールをさらにおいしく
13=アルコールと健康
```

ホームの「章でしぼる」「級でしぼる」で、章×級の組み合わせで出題範囲を選べます。

- `choices` は必ず4つ、`answer` は 0〜3。各問末尾のカンマ `,` を消さない。
- 公式テキストの文章はそのまま転載しない（自分の言葉で作問）。
- 追記したら上の「更新して本番に反映する」手順で push する。

---

## iPhone で使う・ホーム画面に追加する

1. iPhoneのSafariで https://marchisio17.github.io/biaken-drill/ を開く
2. 画面下の共有ボタン（□に↑）をタップ
3. 「ホーム画面に追加」→「追加」
4. ホームのアイコンから起動すると全画面アプリとして動作
5. 一度開けば、Wi-Fiやモバイル回線が無くてもオフラインで使える

---

## ゼロから作り直す場合の手順（参考・通常は不要）

新しいPCや別リポジトリで一から公開し直すときの記録。

```bash
# 1. アプリ専用フォルダを用意してGit初期化
cp -R "/Users/sei/Documents/Claude/Projects/ビールプロジェクト/07_勉強アプリ" ~/biaken-drill
cd ~/biaken-drill
printf ".DS_Store\n" > .gitignore
git init
git branch -M main
git add -A
git commit -m "Initial commit: びあけん対策ドリル PWA"

# 2. GitHubで空のpublicリポジトリ biaken-drill を作成（README等は追加しない）

# 3. リモート登録してpush
git remote add origin https://github.com/marchisio17/biaken-drill.git
git push -u origin main
```

その後、リポジトリの **Settings → Pages → Source: Deploy from a branch → main / (root) → Save** で公開。

---

## 困ったとき

| 症状 | 対処 |
|------|------|
| `git push` でパスワードを聞かれて通らない | GitHubのパスワードではなく **Personal Access Token (PAT)** を貼る。または GitHub Desktop アプリ経由でpushする |
| 公開URLが 404 | 反映に数分かかることがある。少し待って再読み込み。Settings→Pages の Branch が `main` / `(root)` か確認 |
| iPhoneでアプリが古いまま | アプリ／Safariタブを完全に閉じて開き直す。直らなければホームのアイコンを削除→再度「ホーム画面に追加」 |
| 更新したのに本番が変わらない | 編集したのが `~/biaken-drill` 側か確認（控えの `07_勉強アプリ/` を編集しても公開には反映されない）。`git push` 済みかも確認 |
| アプリ更新後にオフライン版が古い | `service-worker.js` の `CACHE_VERSION` の数字を上げてからpushすると、端末側のキャッシュが更新される |
